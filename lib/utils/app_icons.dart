import 'package:flutter/material.dart';

class AppIcons {
  static const Icon menu = Icon(Icons.menu, color: Colors.white);
  static const Icon notification = Icon(Icons.notifications, color: Colors.white);
  static const Icon search = Icon(Icons.search, color: Colors.grey);
  static const Icon location = Icon(Icons.location_on, size: 12, color: Colors.white70);

}
